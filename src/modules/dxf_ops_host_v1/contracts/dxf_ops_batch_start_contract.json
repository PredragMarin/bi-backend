{
  "contract_id": "dxf_ops_batch_start_contract_v0",
  "version": "0.1.0-prototype",
  "status": "prototype",
  "description": "Host-side batch start payload contract for future DXF/OPS package integration. This is a simulator/prototype contract, not a frozen production schema.",
  "source_system": "ERP (Gosoft SAP ASE)",
  "required_top_level_fields": [
    "schema_version",
    "contract_version",
    "batch",
    "rn_list",
    "items",
    "black_box_input",
    "fetch_context",
    "validation"
  ],
  "batch_required_fields": [
    "host_module_id",
    "request_id",
    "source_system",
    "input_mode",
    "dsn_requested",
    "erp_query_id",
    "config_text",
    "config_parsed",
    "config_parse_status"
  ],
  "item_required_fields": [
    "rn",
    "row_count",
    "status",
    "erp_rows"
  ],
  "notes": [
    "Host must not replace DXF/OPS package domain parsing or business logic.",
    "config_text is passed through as a string; config_parsed remains a host placeholder unless a later approved contract says otherwise.",
    "Exact ERP SQL/view shape for work-order fetch is not visible from the repo and remains a placeholder."
  ]
}
